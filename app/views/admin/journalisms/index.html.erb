<%= javascript_include_tag "ckeditor/init" %>
<script>
    <% if flash[:note]%>
    alert('<%=flash[:note]%>');
    <% end%>
</script>
<div>
  <ul class="breadcrumb">
    <li>
      <a href="#">Home</a> <span class="divider">/</span>
    </li>
    <li>
      信息列表
    </li>
  </ul>
</div>
<div class="row-fluid sortable">
  <div class="box span12">
    <div class="box-header well" data-original-title>
      <h2><i class="icon-list-alt"></i> 信息列表</h2>
    </div>
    <div class="box-content">
      <table class="table table-striped table-bordered bootstrap-datatable datatable">
        <thead>
        <tr>
          <th>ID</th>
          <th>标题</th>
          <th>分类</th>
          <th>创建时间</th>
          <th>详细</th>
          <th>删除</th>
        </tr>
        </thead>
        <tbody>
        <% @journalisms.each do |q| %>
            <tr>
              <td><%= q.id %></td>
              <td><%= q.title %></td>
              <td><%= q.question_category_name %></td>
              <td class="center"><%= my_datetime_format q.created_at %></td>
              <td class="center"><%= link_to "详细", admin_journalism_path(q) %></td>
              <td><%= link_to '删除', admin_journalism_path(q), :method => :delete, :confirm => "您确定要删除吗?" %></td>
            </tr>
        <% end %>
        </tbody>
      </table>
      <div class="pagination pagination-centered">
        <ul>
          <%= paginate @journalisms %>
        </ul>
      </div>
      <div class="form-actions">
        <div style="float: left">
          <%= link_to '添加信息', new_admin_journalism_path, :class => "btn btn-primary" %>
        </div>
      </div>
    </div>
  </div>
  <!--/span-->
</div><!--/row-->